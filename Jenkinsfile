// Quickstart v3 <https://github.ibm.com/connections/quickstart>

@Library('quickstart') _

qs {
    plugin = 'default'

    // dockerImageName = 'elasticsearch-cluster'
    // DEBUG_BUILD_ALL = false
    // docker
    // dockerScript = 'scripts/docker_build.sh'
}
